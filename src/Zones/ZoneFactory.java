package Zones;

// Each zone has unique properties, this factory helps encapsulate zones so changes are not required
// within the main body of code

public class ZoneFactory {

    //if in zoneA region



}
